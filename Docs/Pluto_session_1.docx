       
       IP propio(custom visto desde Linux)
       
       
       
    1. Open vivado → open pluto project   pluto.xrp
    2. ip integrator→ open block design
    3. add IP→ Binary_counter + AXI_gpio


    4. save
    5. generate output products
    6. generate bitstream
    7. file→ export→ export hardware(include bitstreams)	


8. hacer make desde la raiz 

   make


9. copiar el resultado de la carpeta build → pluto.frm en el pluto y desmontar

10. Esperar hasta que se vuelva a conectar automaticamente

 

para leer el contador desde linux usar



// cada 1 segundo lee el contador binario conectado a el axi gpio

watch -n 1 "devmem 0x41200000"  


// solo da un valor
devmem 0x41200000


asi hacerlo con cada bloque que se desee si es un custom ip propio se debe crear una interfaz axi lite para que vivado asigne direcciones.
